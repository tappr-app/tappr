const initialState = {
    beer: [],
    isFetching: false,
    error: ''
};

export default beerReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state
  };
};