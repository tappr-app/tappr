const initialState = {
    beer: [],
    isFetching: false,
    error: ''
};

export const beerReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state
  };
};